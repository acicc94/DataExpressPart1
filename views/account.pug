extends layout

block content
    p Name: #{user.Username}
    p Age: #{user.age}
    p Email #{user.email}
    p Question 1 #{user.Q1}
    p Question 2 #{user.Q2}
    p Question 3 #{user.Q3}

    br
               
    p
    #menu
        a(href="/editInfo/" + user.id) Edit account info
    
