extends layout

block content
    div(class='login')
        #login
            a(id='login' href='/login/') login
    
    div(class='signup')
        #signup
            a(id='signup' href='/signUp') signup