html
    head
        link(rel="stylesheet", href="style.css")
        title=title
    body
        header
            nav
             ul(class='menuBlock')
               each item in config.menu
                a(href=item[1], target = item[2])
                    div(class = 'menu_tabs')
                     li(id = item[0]) #{item[0]}
        block content